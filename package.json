{
  "name": "mercury-extension",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.17.1",
    "jsonwebtoken": "^8.1.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0",
    "react-syntax-highlighter": "^7.0.0"
  },
  "config": {
    "pack": "build/. -p test_key.pem -o dist/mercury.crx"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build && node scripts/helpers.js",
    "gen-crx": "./node_modules/.bin/crx pack $npm_package_config_pack",
    "pack": "npm run build && npm run gen-crx",
    "test": "react-app-rewired test --env=jsdom",
    "eject": "react-app-rewired eject"
  },
  "devDependencies": {
    "crx": "^3.2.1",
    "react-app-rewired": "^1.4.1",
    "write-file-webpack-plugin": "^4.2.0"
  }
}
